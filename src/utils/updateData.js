
import { PREFIX } from "./cache"

export default async function updateData (key) {
          const url = key.replace(PREFIX, "")
        //   return await fetchApi(url, {
        //             cacheData: true,
        //             getCachedDataFirst: false,
        //   })
}